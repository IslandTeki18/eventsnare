# Vite env for eventsnare-admin. All VITE_* values are build-time public.
# VITE_CONVEX_URL must be the Convex .convex.cloud deployment domain (not .convex.site).
VITE_CONVEX_URL=
VITE_CLERK_PUBLISHABLE_KEY=
